type Book {
    id: ID
    title: String
    isbn: String
    publishedYear: Int
    author: Author
}

input AddBookDTO {
    title: String!
    isbn: String!
    publishedYear: Int!
    authorId: Int!
}